# GeoBox 地理可视化课件舱 — 技术架构方案与开发路线图

> 版本：v0.1（待确认稿） 2026-09-11
> 定位：开源个人学习项目 / Windows 桌面端 / 面向希沃白板大屏授课场景

---

## 一、产品形态（基于已确认需求）

GeoBox 是一个 **"地理交互课件运行时 + 课件库管理器"**：

- **预置课件**：官方制作的地理难点交互课件（等高线沙盘、地球运动模拟器等），开箱即用；
- **用户课件**：教师用 vibe coding 自行编写的 HTML 网页程序，导入 GeoBox 后统一管理、一键打开、全屏授课；
- **统一包格式**：预置课件与用户课件使用**完全相同的包格式和运行时**，没有二等公民；
- **单文件分享**：任何课件可导出为单个 `.gpak` 文件，双击即可导入另一台电脑；
- **本地优先**：SQLite 本地存储，完全离线可用；架构预留云端 API 抽象层，未来平滑接入社区。

## 二、技术栈决策

| 层 | 选型 | 理由 |
|---|---|---|
| 桌面壳 | **Electron**（Windows x64，NSIS 安装包 + 便携 zip） | vibe coding 效率最高；与未来云端社区衔接最顺；希沃白板（Win10/i5/8G）性能够用 |
| 前端 | **React 18 + TypeScript + Vite** | 组件生态成熟，AI 辅助编程资料最多 |
| 3D/可视化 | **Three.js**（课件运行时内）+ ECharts（数据图表） | 地理可视化事实标准 |
| 本地后端 | **Electron 主进程服务层 + better-sqlite3** | 同步 API、零配置、单文件数据库；服务层接口化，即云端抽象层 |
| 包格式 | **`.gpak`（ZIP 容器）** | 见下文 |
| 地图数据 | **GeoJSON 直接解析；DEM（GeoTIFF/ASC）用 geotiff.js 在课件内解析** | 课件沙箱内即可完成，无需原生依赖 |

**决策说明**：此前讨论过 Tauri（更轻），但希沃白板硬件普遍为 i5+8G，Electron 可流畅运行；且 Electron 的 Node 生态对"本地后端 + 未来云同步"最友好，vibe coding 效率优先，故选 Electron。

## 三、课件包格式 `.gpak`（核心设计）

一个 `.gpak` 就是一个 ZIP，结构：

```
课件名.gpak
├── manifest.json      # 元数据：id/名称/作者/学科/标签/版本/封面/入口
├── index.html         # 入口页面
├── assets/            # JS/CSS/图片/数据文件
└── thumbnail.png      # 封面缩略图（可选）
```

`manifest.json` 示例：

```json
{
  "id": "contour-sandbox",
  "name": "等高线 3D 沙盘",
  "author": "GeoBox 官方",
  "version": "1.0.0",
  "subject": "地理",
  "tags": ["等高线", "地形", "初中"],
  "entry": "index.html",
  "permissions": ["file-import"]
}
```

- 用户把自己 vibe coding 的网页项目打成 zip 改名为 `.gpak`（补一个 manifest）即可导入；应用内也提供"从文件夹导入"，自动生成 manifest。
- 导出 = 从库中重新打包为 `.gpak`，并注册 Windows 文件关联（双击 `.gpak` 唤起 GeoBox 导入）。

## 四、运行时与桥接 API

课件在**沙箱化 WebView**（独立 BrowserWindow，`contextIsolation` 开启）中全屏运行，通过 preload 注入桥接对象 `window.geobox`：

```js
window.geobox.importFile({ accept: ".tif,.asc,.geojson" }) // 调起系统对话框导入真实地理数据
window.geobox.storage.set(key, value)                      // 课件级持久化（存 SQLite）
window.geobox.setFullscreen(true)
window.geobox.platform   // "geobox/x.y.z"，课件可检测运行环境
```

- 普通 HTML 课件不调用桥接也能跑（纯网页兼容）；
- 需要真实地理数据导入的课件（如等高线沙盘导入本地 DEM）通过 `file-import` 权限声明 + 桥接实现；
- 授课场景优化：全屏 kiosk 模式、触控手势适配、大字号 UI、无网络依赖。

## 五、本地后端与云端抽象层

```
渲染进程(React 课件库 UI)
   │  IPC
主进程服务层（CoursewareService / StorageService / ImportExportService）
   │  实现接口
CoursewareRepository 接口 ←—— 云端抽象层
   ├── LocalRepository（MVP：SQLite + 本地文件目录）
   └── CloudRepository（未来：账号/云同步/社区，实现同一接口）
```

MVP 只实现 `LocalRepository`；社区功能上线时新增 `CloudRepository`，UI 层零改动。

## 六、数据目录

```
%APPDATA%/GeoBox/
├── geobox.db          # SQLite：课件元数据、设置、课件存储
└── packages/          # 解压后的课件包（按 id 分目录）
```

## 七、开发路线图

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **P0 骨架** | Electron+Vite+React 工程；SQLite 初始化；课件库 UI（网格/搜索/标签）；导入 zip/文件夹；`.gpak` 导出与文件关联 | 能导入一个第三方 HTML 课件并全屏打开 |
| **P1 首个预置课件** | 等高线 3D 沙盘：程序生成虚拟地形 + 实时等高线投影 + 任意剖面切割 + **DEM/GeoJSON 真实数据导入** | 一节课可直接用；打包为 `.gpak` 走通全流程 |
| **P2 预置课件扩充** | 地球自转公转/晨昏线模拟器、时区换算器、水循环动画 | 预置课件 ≥ 4 个 |
| **P3 体验完善** | 课件详情页、版本管理、更新检测、设置页、便携版打包 | 可发布 v1.0 |
| **P4（远期）** | CloudRepository + 账号 + 社区共享 | 另立项讨论 |

**MVP = P0 + P1。**

## 八、待你确认的小决策

1. **名称**：暂用 GeoBox（地理课件舱），可改；
2. **开源协议**：建议 MIT（最宽松，适合学习项目），或 Apache-2.0（多专利保护）；
3. **课件桥接 API 命名**：`window.geobox.*`，随名称调整；
4. 首个预置课件（P1）就是**等高线 3D 沙盘**，与之前调研结论一致——如无异议，P0 完成后直接做它。
